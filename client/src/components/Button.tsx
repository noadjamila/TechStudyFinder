import React from 'react';
//import './Button.css';

const Button: React.FC = () => {
    return (
        <button className="custom-button">
            Quiz Starten
        </button>
    );
};

export default Button;
