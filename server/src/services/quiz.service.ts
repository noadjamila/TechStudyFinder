import { getFilteredResults } from '../repositories/quiz.repository';

export async function filterLevel1(newAnswers: any[]) {
  // Implementiere die Logik für Level 1 Filterung hier
}

export async function filterLevel2(studyProgrammeIds: number[] | undefined, newAnswers: any[]) {
  // Implementiere die Logik für Level 2 Filterung hier
}

export async function filterLevel3(studyProgrammeIds: number[] | undefined, newAnswers: any[]) {
  // Implementiere die Logik für Level 3 Filterung hier
}
